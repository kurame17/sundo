<%= form_for(@auction_post) do |f| %>
  <% if @auction_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auction_post.errors.count, "error") %> prohibited this auction_post from being saved:</h2>

      <ul>
      <% @auction_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.label :content %><br>
      <%= f.text_area :content %>
    </div>
    <div class="field">
      <%= f.label :img %><br>
      <%= f.text_field :img %>
    </div>

    <div class="field">
      <%= f.label :category %><br>
      <%= f.select(:category) do %>
        <% [['가전제품', "가전제품"], ['육아용품', "육아용품"],['가구', "가구"]].each do |c| -%>
          <%= content_tag(:option, c.first, value: c.last) %>
        <% end %>
      <% end %>
    </select>
  </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
